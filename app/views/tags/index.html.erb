<h1>All Tags:</h1>

<ul class="comment">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), id: "tag_#{tag.id}" %>
      
    </li>
  <% end %>
</ul>
<%= link_to "<< Back to Articles List", articles_path %> 

